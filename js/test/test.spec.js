import expect from 'expect';

function setup() {
  
}

describe('components', () => {
  describe('Header', () => {
    it('should render correctly', () => {
      expect(true).toEqual(true);
    });
  });
});
